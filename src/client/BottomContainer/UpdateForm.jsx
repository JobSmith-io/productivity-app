import React from 'react';

// Display when user want's to update a field
// Should there be seperate components for updating different fields
// Needs to send put request to db
  // also needs to update jobs state
// Should close UpdateForm on submit
const UpdateForm = () => {
  return (
    <div>
      Update form
    </div>
  )
}

export default UpdateForm;